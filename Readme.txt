»сполн€емым файлом €вл€етс€ Main.m

—троитс€ траектори€ двумерной системы канонического вида, описывающей колебани€ ма€тника (по€снени€ есть в файле
ћа€тник.docx).

≈сли траектори€ не попадает в ограничени€ на функцию и ее производную вида

constr1 < Psi(y) < constr2

dy_constr1 < dPsi(y) / dy < dy_constr2

(возможно, что не все 4 ограничени€ заданы, то крива€ корректируетс€).

—начала корректируетс€ выход за ограничени€ по dPsi(y) c с помощью вызова методов lower_constraint_dPsi(...)
и upper_constraint_dPsi(...)

«атем корректируетс€ выход за ограничени€ по Psi(y). ƒл€ этого нужно вызвать методы lower_constraint_Psi(...)
и upper_constraint_Psi(...)

ѕосле вызова любого из методов нужно добавить в массив control_arr строку row, котора€ полностью описывает 
корректирующий участок траектории. Ёто нужно дл€ того, чтобы затем автоматически синтезировать новое управление.

ћассив control_arr имеет следующую структуру.

 ажда€ его строка описывает кривую.

ѕор€док элементов в строке следующий:

1 : y_0 - начальна€ точка (y) кривой;
2 : y_end - конечна€ точка (у) кривой;
3 - 6 : коэффициенты c0, c1, c2, c3
7 : тип кривой
крива€ может иметь тип 0, 1, 2
тип 0 - крива€ вида Psi(y) = c0 + c1*(y - y_0_c) + c2*(y - y_0_c)^2 + c3*(y - y_0_c)^3;
тип 1 - крива€ вида Psi(y) = c0 + c1*(y - y_0) + c2*(y - y_0)^2 + c3*(y - y_0)^3 + d*(y - y0)^2*(y - y_end)^2
тип 2 - крива€ вида Psi(y) = c0 + c1*(y - y_0_c) + c2*(y - y_0_c)^2 + c3*(y -y_0_c)^3 + d*y_norm^2*(3 - 2*y_norm), 
где y_norm = (y - y_0) / (y_end - y_0);
8 : d (если есть)
9 : y_0_c (если есть)
10 : врем€ движени€ по этому участку кривой (вычисл€етс€ автоматически в time_modeling.m)

¬ Main.m в массив control_arr просто добавл€ютс€ строки по мере их по€влени€.

¬ time_modeling.m массив автоматически сортируетс€ и вычисл€етс€ вс€ необходима€
информаци€ дл€ построени€ управлени€.